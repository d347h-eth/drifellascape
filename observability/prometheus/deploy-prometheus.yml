global:
    scrape_interval: 5s
    evaluation_interval: 5s

scrape_configs:
    - job_name: "drifellascape-backend"
      metrics_path: /metrics
      static_configs:
          - targets: ["backend:42840"]
            labels:
                runtime: "backend-api"

    - job_name: "drifellascape-worker"
      metrics_path: /metrics
      static_configs:
          - targets: ["worker:42841"]
            labels:
                runtime: "sync-worker"
