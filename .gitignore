# Personal VS Code settings (keep workspace settings)
.vscode/launch.json
.vscode/tasks.json
.vscode/*.code-workspace

# Dependencies (rarely used with Yarn PnP, but safe to ignore if created)
node_modules/

# Yarn Berry (commit selected subdirs only)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Yarn PnP artifacts
.pnp.cjs
.pnp.loader.mjs

.env

# Builds and caches
dist/
build/
*.tsbuildinfo

# Logs
logs/

# OS
.DS_Store
Thumbs.db

# SQLite database files (consolidated under dedicated folder)
database/drifellascape.db/*
!database/drifellascape.db/.gitkeep

metadata/
static/

# for local development (supposed to have a symlink like: "ln -s ../static static")
frontend/public/
# in real deployment: "frontend/static" is mounted into container and served by Caddy with a wildcard route
frontend/static/art/

releases/*
!releases/.gitkeep
